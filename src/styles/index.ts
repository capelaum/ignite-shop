import { createStitches } from '@stitches/react'

export const {
  config,
  theme,
  styled,
  css,
  globalCss,
  keyframes,
  getCssText,
  createTheme,
} = createStitches({
  theme: {
    colors: {
      primary: '#1e1e1e',
      green: '#00875F',
      greenLight: '#00B37E',
      gray1: '#121214',
      gray2: '#202024',
      gray3: '#C4C4CC',
      gray4: '#E1E1E6',
    },
  },
})
